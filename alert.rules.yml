groups:
  - name: sentiment-model-drift
    rules:
      - alert: ModelConfidenceDrift
        expr: prediction_confidence_score < 0.563
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Model confidence drift detected"
          description: "Prediction confidence stayed below 0.563 for more than 1 minute."
